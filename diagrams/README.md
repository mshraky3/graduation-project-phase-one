# Diagrams

DFDs, sequence, use case, architecture and data model diagrams. Keep the editable source (e.g. `.drawio`) next to the exported PNG/SVG. Name files by task ID, e.g. `B3-dfd-level1.drawio`.
