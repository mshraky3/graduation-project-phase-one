# Graduation Project — Phase 1

**Cross-Device AI Assistant** (working title — the project is not named yet)

An AI assistant that runs locally on the user's strongest device and connects their PC, laptop, phone and tablet directly (peer-to-peer) over the local network, with no internet and no cloud server. It understands files and data across all devices at once.

## Features
1. **Duplicate file detection** — identical or similar files stored on more than one device
2. **Unified search** — search from any device and find a file on another
3. **Password management** — every account and its password across all devices, in one place
4. **Clipboard sync** — copy on one device, paste on another
5. **Screen sharing & remote control** — view and control one device from another
6. **Extra screen** — use a tablet or another device as a second monitor
7. **Direct file sharing** — send files between devices over the local network
8. **Offline records** — see a device's files even when it is switched off (last known state)

## Phase 1 scope
Analysis and design only; no code is delivered in this phase. The report follows the supervisor's structure:
1. Introduction
2. Literature Review
3. Problem Analysis
4. System Design

## Picking your tasks
👉 **Task board:** https://mshraky3.github.io/graduation-project-phase-one/

1. Accept the collaborator invitation to this repository.
2. Comment `/register Your Full Name` on the **Team registration** issue.
3. Open a task issue and comment `/claim`. To give it back, comment `/unclaim`.

Each person can hold up to 15 points (S = 1, M = 2, L = 3). A task has exactly one owner, and the first valid claim wins.

The full breakdown is also in [docs/planning/Phase1_Task_Breakdown.pdf](docs/planning/Phase1_Task_Breakdown.pdf).

## Repository layout
| Folder | Contents |
|---|---|
| `docs/proposal/` | The proposal the supervisor approved |
| `docs/planning/` | Task breakdown and schedule |
| `research/` | Notes and sources for the literature review |
| `diagrams/` | DFDs, sequence, use case, architecture and data model diagrams (source files plus exported images) |
| `report/` | Report chapters |

## Working together
- Put your work in the matching folder and name files by task ID, e.g. `diagrams/B3-dfd-level1.drawio`.
- Commit often with clear messages, e.g. `B3: add Level 1 DFD draft`.
- Link your commits to the task by mentioning its issue number, e.g. `#12`.
